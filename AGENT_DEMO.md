# 图书检索与推荐 Agent

这个本地演示把目录检索、用户偏好排序和详情核验连成一个工具调用流程。LLM 负责解析自然语言并选择工具；候选召回、排序和图书事实来自仓库 CSV 与 Python 代码。工具只读，不连接 MySQL，也不会下载图书图片。

## 安装

在仓库根目录运行：

```powershell
python -m pip install -r requirements-agent.txt
Copy-Item .env.example .env
```

真实模型评测可使用 DeepSeek 或 OpenAI Responses API。使用 DeepSeek 时，在本机 `.env` 配置 `DEEPSEEK_API_KEY`，并可设置 `DEEPSEEK_MODEL=deepseek-flash`、`DEEPSEEK_BASE_URL=https://api.deepseek.com`；使用 OpenAI 时配置 `OPENAI_API_KEY` 与 `OPENAI_MODEL`。请勿把 `.env` 提交到 Git，也不要把 API key 粘贴到聊天或评测报告中。命令会读取本机环境变量或仓库根目录 `.env`；报告只记录模型名、token 用量、耗时、工具参数和工具结果。

DeepSeek 的 Responses API 不保留跨请求状态，因此适配器会在每次调用时重放本轮对话和工具结果。DeepSeek 目前会忽略 `parallel_tool_calls` 设置；Agent 通过每轮只开放当前阶段的一个工具，并拒绝额外并行调用，保证调用顺序可核验。

## 本地检索与排序演示

```powershell
python -m book_agent.cli demo `
  --query 算法 `
  --user-id U00001 `
  --category 科技 `
  --keyword 算法 `
  --max-price 150 `
  --limit 2
```

演示先用字符 n-gram TF-IDF 召回目录候选，再按该用户的交互历史和画像排序，最后读取详情。详情工具仅返回书名、作者、类别、关键词、简介和价格等回答所需字段。未知用户会回传用户上下文不可用的状态，Agent 不应声称结果经过了个性化。

## 现场自由提问

配置好 `.env` 后，可以启动真实模型交互。每次输入会独立处理，不保留上一个问题的对话上下文；输入 `q`、`exit` 或 `退出` 结束：

```powershell
python -m book_agent.cli ask --provider deepseek
```

也可以直接传一个问题，回答后命令退出：

```powershell
python -m book_agent.cli ask `
  --provider deepseek `
  --query "结合 U00001 的阅读历史，推荐两本 150 元以内的科技类算法书，并说明依据"
```

每次回答后会显示 Agent 的工具调用轨迹、个性化信号状态、安全回答保护状态和耗时。安全兜底会从目录类别、关键词、价格或文本检索分数中生成可核验的理由；目录缺少年龄适用范围时，Agent 会说明无法核验，不会把普通候选说成适合幼儿。该命令会实际调用模型 API 并使用账户额度；本地 `demo` 命令不调用模型。

## 推荐离线评测

```powershell
python -m book_agent.cli evaluate-recommender --ks 5 10 20
```

评测按每位用户的时间顺序留出最后一条不同图书交互，只在其余历史上拟合热门度与 ItemCF。默认报告写到被 Git 忽略的 `reports/recommender_eval.json`。

本地 94,934 条交互的实测切分包含 84,375 条训练交互、9,989 位测试用户和 10 位不足两本不同图书而跳过的用户。测得结果如下：

| 方法 | Recall@20 | NDCG@20 | MRR@20 |
| --- | ---: | ---: | ---: |
| 热门度 | 0.0061 | 0.0021 | 0.0010 |
| ItemCF | 0.0123 | 0.0045 | 0.0024 |

这是仓库现有交互数据上的离线留一结果，不代表线上转化或真实用户收益；这批交互数据主要是模拟数据。指标用于建立可复现的推荐基线，不应当写成业务效果提升。

## 真实模型 Agent 案例评测

评测集包含 25 条固定 JSONL 案例（[完整案例](data/agent_eval_cases.jsonl)），覆盖个性化推荐、类别/关键词/价格筛选、双本推荐、未知用户、需要澄清、无结果和目录不含星级评分等情况。每条案例记录预期工具序列、结果类型和筛选约束。代表性输入如下：

| 类型 | 输入示例 | 预期处理 |
| --- | --- | --- |
| 正常推荐 | `结合 U00001 的阅读历史，推荐科技类、关键词包含算法、价格不超过 150 元的书，并说明为什么适合。` | 检索、个性化排序、详情核验后推荐 |
| 需要澄清 | `结合我的阅读历史推荐两本适合入门机器学习、偏实践的书。` | 缺少用户编号，不调用工具并询问用户 |
| 无结果 | `只搜索类别为“量子烹饪学”的图书，如果找不到就直接说明。` | 检索结果为空，明确回复无结果 |

运行完整评测：

```powershell
python -m book_agent.cli evaluate-agent `
  --provider deepseek `
  --cases data/agent_eval_cases.jsonl `
  --output reports/agent_eval_final.json
```

评测脚本 `book_agent/agent_eval.py` 对每条结果按预期类型判定：

- **推荐：** 工具序列符合预期；返回数量达到案例要求；每本书都来自详情工具核验结果；类别、关键词和价格满足筛选条件；回答中的目录事实有证据支持；个性化表述与用户历史状态一致。
- **澄清：** 不调用工具、不引用书目，针对缺失用户编号或目录没有的评分字段提出澄清。
- **无结果：** 执行目录检索，候选为空且没有读取详情，并明确告知用户没有符合条件的书。

最终通过表示**模型回答或安全兜底后的答案**满足案例标准；报告同时单独记录兜底是否触发。评测集没有 API/网络故障案例，因此这些结果不代表异常服务恢复能力。

### 本次真实模型结果

使用 DeepSeek `deepseek-flash` 于 2026-09-23 运行一次。汇总来自逐案评测报告；原始报告位于 Git 忽略的 `reports/agent_eval_final.json`，不会提交 API Key 或原始轨迹。

| 预期类型 | 案例数 | 首答 / 处理路径 | 最终符合预期 |
| --- | ---: | --- | ---: |
| 推荐 | 20 | 12 条首答直接通过；8 条因包含目录无法证实的事实，被拦截并由已核验详情重建 | 20/20 |
| 澄清 | 2 | 2 条均未调用工具，按预期追问 | 2/2 |
| 无结果 | 3 | 3 条均检索到空候选，由无结果兜底明确回复 | 3/3 |
| API / 网络异常 | 0 | 本次固定案例未覆盖此类异常 | 未评估 |
| **合计** | **25** | **安全兜底触发 11/25（44%）** | **25/25** |

因此，推荐任务的首答直接通过率为 12/20（60%）；最终 20 条推荐结果中有 8 条依赖事实校验和安全兜底。25/25 表示这一次运行的最终流程按固定案例标准完成，不表示模型首答准确率为 100%，也不代表线上用户收益。真实模型结果受模型版本和采样影响，复测可能变化。

### 模块测试

单元测试与上述真实模型任务评测分开统计。Agent 模块共有 53 条 `pytest` 用例通过，覆盖参数校验、工具阶段、证据核验、事实校验、澄清和兜底逻辑；模块测试不能替代真实模型任务效果评测。

```powershell
python -m pytest -q tests/book_agent
```

运行前请确认本机 `.env` 已配置 DeepSeek Key。不要把 `.env`、API Key 或评测报告中的用户查询数据提交到公开仓库。

## 简历表述参考

> 构建图书检索与推荐 Agent，编排目录召回、用户偏好排序和详情核验 3 个只读工具；实现严格 JSON Schema 参数校验、候选来源约束、目录标签归一化、事实一致性校验和安全回答兜底。使用 DeepSeek `deepseek-flash` 对 25 条固定案例进行一次真实模型评测：推荐 20/20 符合预期（12 条首答直接通过、8 条经事实校验与兜底后通过），澄清 2/2、无结果 3/3 均符合预期；评测集未覆盖 API / 网络异常。另有 53 条 pytest 模块用例通过。

推荐系统离线指标可以单独陈述为本地模拟交互数据的留一评测结果，不要把它和 Agent 案例成功率混为一谈。
